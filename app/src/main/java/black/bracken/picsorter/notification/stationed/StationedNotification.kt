package black.bracken.picsorter.notification.stationed

import black.bracken.picsorter.notification.NotificationView

class StationedNotification(
    override val presenter: StationedContract.Presenter
) : NotificationView<StationedContract.View, StationedContract.Presenter> {

    override fun show() {
        TODO("not implemented yet")
    }

}